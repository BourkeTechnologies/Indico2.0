@model Indico20.Models.NavigationModel
@using System.Security.Policy
@using System.Web.Mvc.Html
@using Indico20.Models


<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">
                <img src="~/Content/img/logo_transparent.png" alt="">
            </a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                @foreach (NavigationModel item in ViewBag.Items)
                {
                    if (item.Parent == 0)
                    {
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                @item.Title
                            </a>
                            @if (item.SubMenus.Count > 0)
                            {
                                <ul class="dropdown-menu">
                                    @foreach (var submenu in item.SubMenus)
                                    {
                                        <li>
                                            <a href="@Url.Action(submenu.Action, submenu.Controller)">@submenu.Title</a>
                                        </li>
                                    }

                                </ul>
                            }

                        </li>
                    }

                    <li>
                        <a href="@Url.Action(item.Action, item.Controller)">
                        </a>
                    </li>
                }
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span>@ViewBag.UerName</span><img src="~/Content/img/nouser-64px-64px.png" alt="Gravatar" class="nav-gravatar img-circle" style="margin-left: 12px; margin-top: -10px;"><span class="caret"></span> </a>
                    <ul class="dropdown-menu pull-right">
                        <li><a href="#">Profile</a></li>
                        <li site="">@Html.ActionLink("Logout", "Logout", "Auth")</li>
                    </ul>
                </li>
            </ul>
        </div>

        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>

@*<nav class="navbar navbar-custom navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @*<a class="navbar-brand" href="#">Remote Console Portal</a>
            <a title="Home" class="brand" href="javascript:void(0);">
                <img src="~/Content/img/SCSI_logo_51x33.png"  style="margin-top: 0px; margin-left: -14px; margin-top: 1px;" alt="Brand" default="/content/img/nologo-122px-33px.png" />
            </a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                @foreach (UserMenuItemRoleViewBO topMenu in Model.TopLeftMenuItems)
                {
                   // string strStyle = (((topMenu.Controller != null) && (Model.ParentMenuItemID == (int)topMenu.MenuItem)) ? "active" : string.Empty);
                    string linkText = topMenu.MenuName; /*NavigationModel.GetLocalizedMenuName(topMenu.Key).Trim();*/
                    if (String.IsNullOrEmpty(linkText))
                    {
                        RemoteConsolePortalLogging.log.ErrorFormat(Model.LoggedUser.ID.ToString(), "Error occurred while populating Secondry Navigation. Resource value is empty. Key : " + topMenu.Key);
                        Response.Redirect("/Error/Cannotcontinued");
                    }
                    else
                    {
                        if (topMenu.IsTopNav == true)
                        {
                            List<UserMenuItemRoleViewBO> lstSubMenus = Model.SubMenuItems.Where(o => o.Parent == topMenu.MenuItem).ToList();
                            if (lstSubMenus.Count == 0)
                            {
                                <li site="">@Html.ActionLink(topMenu.MenuName, topMenu.Action, topMenu.Controller)</li>
                            }
                            else {
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">@topMenu.Title <span class="caret"></span></a>
                                    <ul class="dropdown-menu">
                                        @foreach (UserMenuItemRoleViewBO subMenu in lstSubMenus)
                                        {
                                            <li>@Html.ActionLink(subMenu.Title, subMenu.Action, subMenu.Controller)</li>
                                        }
                                    </ul>
                                </li>
                            }
                        }
                    }
                }
            </ul>*@